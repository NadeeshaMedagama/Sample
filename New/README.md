# New - JavaScript Fundamentals 📘

This folder contains comprehensive JavaScript examples covering fundamental programming concepts.

## 📄 Files

### `basic.js`

A comprehensive JavaScript file containing examples and exercises covering:

## 📚 Topics Covered

### 1. Variables and Declarations
- `var`, `let`, and `const` keywords
- Variable scope and hoisting
- Best practices for variable naming

### 2. Data Types
- Primitive types: `string`, `number`, `boolean`
- Type checking with `typeof`
- Type conversion (`Number()`, `String()`, `Boolean()`)

### 3. User Interaction
- `window.prompt()` for user input
- `window.alert()` for displaying messages
- `console.log()` for debugging

### 4. DOM Manipulation
- `document.getElementById()` to select elements
- `.innerHTML` to modify content
- `.value` to get input values
- Event handling with `.onclick`

### 5. Operators
- Arithmetic operators (`+`, `-`, `*`, `/`, `%`, `**`)
- Assignment operators (`+=`, `-=`, `*=`, `/=`)
- Comparison operators (`==`, `===`, `!=`, `!==`, `<`, `>`)
- Logical operators (`&&`, `||`, `!`)

### 6. Math Object
- `Math.round()` - Round to nearest integer
- `Math.floor()` - Round down
- `Math.ceil()` - Round up
- `Math.pow()` - Power calculation
- `Math.sqrt()` - Square root
- `Math.abs()` - Absolute value
- `Math.max()` / `Math.min()` - Find max/min values

### 7. String Methods
- `.charAt()` - Get character at index
- `.indexOf()` / `.lastIndexOf()` - Find position
- `.length` - Get string length
- `.trim()` - Remove whitespace
- `.toUpperCase()` / `.toLowerCase()` - Case conversion
- `.slice()` - Extract substring

### 8. Conditional Statements
- `if`, `else if`, `else` statements
- Ternary operator (`condition ? true : false`)
- `switch` statements

### 9. Loops
- `for` loops
- `while` loops
- `do...while` loops

### 10. Functions
- Function declarations
- Function expressions
- Arrow functions (`=>`)
- Default parameters
- Return values

### 11. Arrays
- Array creation and indexing
- Array methods:
  - `.push()` / `.pop()` - Add/remove from end
  - `.shift()` / `.unshift()` - Add/remove from beginning
  - `.length` - Array length
  - `.indexOf()` - Find element index
  - `.includes()` - Check if element exists
  - `.reverse()` - Reverse array
  - `.sort()` - Sort array

## 🏃 Running the Code

Some examples require an HTML file with specific elements. For standalone examples:

```bash
node basic.js
```

**Note:** DOM-related examples will only work in a browser environment with the appropriate HTML structure.

## 💡 Learning Tips

1. Run each section separately to understand individual concepts
2. Modify the values and observe the changes
3. Use `console.log()` to debug and understand the flow
4. Try creating your own variations of each example

## 📝 Notes

- Some code may be commented out to prevent conflicts
- Variable redeclarations are intentional for demonstration purposes
- Browser-specific functions (`alert`, `prompt`) won't work in Node.js
